# Codex Local Skills

Esta carpeta replica las skills locales definidas en `.claude/skills/` para que tambien esten disponibles desde una convencion `.codex/`.

Si se actualiza una skill en uno de los dos sitios, conviene reflejar el mismo cambio en el otro para evitar divergencias.
