display_name: JSON Canvas
short_description: Create or edit .canvas files with clear node structure and relationships.
default_prompt: Draft or update a JSON Canvas file that is valid, readable, and consistent with the vault's note structure.
